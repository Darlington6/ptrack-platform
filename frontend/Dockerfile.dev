FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json .npmrc ./
RUN npm ci

# Source code is mounted as a volume at runtime — don't copy here
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]